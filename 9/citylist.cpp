#include "citylist.h"
#include <QString>
#include <QFileDialog>
#include <QMessageBox>
#include <QMap>
#include <QTextStream>
#include <QFile>
#include <QDebug>

void CityList::getCitiesArray(QString text)
{
////    QStringList cities = text.split(",");

////    QRegExp rx("\\d*\\D\\d*");
////    QStringList city = cities.filter(rx);
////    QString list = "";

////    for (int i = 0; i < city.length(); i++) {
////        list += city[i] + ', ';
////    }

//    QMessageBox::warning(this, "title", "msg");
}

